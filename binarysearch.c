#include<stdio.h>
void main()
{
     int n,x,i,mid,c=0,c++,a[50];
     int left,right,flag;
     printf("enter the size of the array");
     scanf("%d",&n);
     printf("enter the elements\n");
     for(i=0;i<n;i++)
     {
     scanf("%d",&a[i]);
     }
     printf("enter the number to be searched");
     scanf("%d",&x);
     mid=(a[0]+a[n-1])/2;
     if (mid==x)
     {
     for(i=n/2;i<n;i++)
     {
     if(x==a[i])
     
     
     
     
     


